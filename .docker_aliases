alias dr='docker run -it '
alias dok='docker'
alias dokb='docker build'
alias dokr='docker run'
alias doklia='docker image ls --all'
alias dora='docker stop $(docker ps -q) && docker rm -vf $(docker ps -a -q) && docker rmi -f $(docker images -a -q) &&  docker system prune -f'
