alias dok='docker'
alias dokb='docker build'
alias dokr='docker run'
alias doklia='docker image ls --all'
alias dora='docker rmi -f $(docker images -a -q) && docker rm -vf $(docker ps -a -q) && docker system prune -f'
